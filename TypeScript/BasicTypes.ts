const number: number = 42; // number type
const string = "Hello, TypeScript"; // string type
let trueValue = true; // boolean type


let myArray: number[] = [1, 2, 3, 4, 5]; // array of numbers

// myArray.push("QWERTY");   <-- This will cause a type error because "QWERTY" is not a number
// myArray[2] = "CCCCC";     <-- This will also cause a type error for the same reason
